import React from 'react';
import PropTypes from 'prop-types';

function WebUISettings({ settings }) {
    return (
        <div>
            WebUISettings
        </div>
    )
}

WebUISettings.propTypes = {
    settings: PropTypes.object.isRequired,
}

export default WebUISettings;