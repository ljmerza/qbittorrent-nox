import React from 'react';
import PropTypes from 'prop-types';

function SpeedSettings({ settings }) {
    return (
        <div>
            SpeedSettings
        </div>
    )
}

SpeedSettings.propTypes = {
    settings: PropTypes.object.isRequired,
}

export default SpeedSettings;